# Sei libero di implementare la funzione mia_media come vuoi, purché
# restituisca la media di tutti i numeri in `mio_vettore`.
# 
# Suggerimento n. 1: sum() restituisce la somma di un vettore.
# Es: sum(c (1, 2, 3)) restituisce 6
# 
# Suggerimento n. 2: lengh() restituisce la dimensione di un vettore.
# Es: length(c (1, 2, 3)) restituisce
# 
# Suggerimento n. 3: la media di tutti i numeri in un vettore è uguale alla somma di tutti
# i numeri nel vettore divisi per le dimensioni del vettore.
# 
# Nota per quelli di voi che si sentono furbissimi: per favore non usare la media ()
# funzione mentre scrivi questa funzione. Stiamo cercando di insegnarti qualcosa
# qui!
#   
#   Assicurati di salvare questo script e digitare submit() nella console dopo aver fatto
# le tue modifiche.

mia_media <- function(mio_vettore) {
  # Scrivi qui il tuo codice!
  # Ricorda: sarà restituita l'ultima espressione elaborata 
}
